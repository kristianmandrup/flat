const {
  leaf
} = require('../leaf')

describe('option: onValueAt', () => {
  it('not a function', () => {})

  it('function sets undefined to an string', () => {})

  it('function sets undefined to an object', () => {})
})
