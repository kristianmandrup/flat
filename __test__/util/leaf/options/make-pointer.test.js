const {
  leaf
} = require('..')

describe('option: makePointer', () => {
  it('not a function', () => {})

  it('function does NOT return an object', () => {})

  it('function does return an object', () => {})
})
