const {
  leaf
} = require('../leaf')

describe('option: keyName', () => {
  it('not a function', () => {})

  it('function does NOT transform key to string', () => {})

  it('function does transform key to string', () => {})
})

describe('option: keynames (compatibility)', () => {
  it('not a function', () => {})

  it('function does NOT transform key to string', () => {})

  it('function does transform key to string', () => {})
})
