const assert = require('assert')
const {
  unflatten
} = require('../flat')

const log = console.log

suite('Unflatten: delimiter', function () {
  test('delimiter not valid', function () {})
  test('camelCase not valid', function () {})
  test('custom valid delimiter', function () {})
})
