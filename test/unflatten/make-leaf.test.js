const assert = require('assert')
const {
  unflatten
} = require('../flat')

const log = console.log

suite('Unflatten: makeLeaf', function () {
  test('makeLeaf not a function', function () {})
  test('custom makeLeaf function', function () {})
})
