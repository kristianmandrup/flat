const assert = require('assert')
const {
  unflatten
} = require('../flat')

const log = console.log

suite('Unflatten: buildPath', function () {
  test('buildPath not a function', function () {})
  test('buildPath returns invalid path', function () {})
  test('custom valid buildPath function', function () {})
})
