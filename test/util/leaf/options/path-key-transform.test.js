const assert = require('assert')
const {
  leaf
} = require('../leaf')

suite('option: pathKeyTransform', function () {
  test('not a function', function () {})

  test('function does NOT transform key to string', function () {})

  test('function does transform key to string', function () {})
})
