const assert = require('assert')
const {
  leaf
} = require('../leaf')

suite('option: nextDepth', function () {
  test('not a function', function () {})

  test('function sets depth to invalid value', function () {})

  test('function increases depth', function () {})

  test('function decreases depth', function () {})
})
