const assert = require('assert')
const {
  leaf
} = require('../leaf')

suite('option: startDepth', function () {
  test('not a number', function () {})

  test('start at 1', function () {})

  test('starts at -1', function () {})
})
