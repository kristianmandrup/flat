const assert = require('assert')
const {
  leaf
} = require('../leaf')

suite('option: onValueAt', function () {
  test('not a function', function () {})

  test('function sets undefined to an string', function () {})

  test('function sets undefined to an object', function () {})
})
