const assert = require('assert')
const {
  leaf
} = require('../leaf')

suite('option: makePointer', function () {
  test('not a function', function () {})

  test('function does NOT return an object', function () {})

  test('function does return an object', function () {})
})
